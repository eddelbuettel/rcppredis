useDynLib(RcppRedis)                    # this package's shared library
import(methods,Rcpp) 	                # packages we require
exportPattern("^[[:alpha:]]+")          # export all identifiers starting with letters
