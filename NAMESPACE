useDynLib(RcppRedis, .registration=TRUE)	# this package's shared library
import(methods,Rcpp,RApiSerialize,RcppMsgPack)	# packages we require
exportPattern("^[[:alpha:]]+")          	# export all identifiers starting with letters
