PKG_LIBS = `$(R_HOME)/bin/Rscript -e "Rcpp:::LdFlags()"`

HIREDIS_OBJECTS = hiredis/net.o hiredis/hiredis.o hiredis/sds.o hiredis/async.o
CPP_SOURCES = $(wildcard *.cpp) $(wildcard gen/*.cpp)
OBJECTS = $(CPP_SOURCES:.cpp=.o) $(HIREDIS_OBJECTS)
